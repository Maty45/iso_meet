{
  "name": "iso-meet",
  "private": true,
  "version": "0.1.0",
  "description": "Minecraft isométrico + oficina virtual + Google Meet",
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "type": "module",
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm:dev -w client\" \"npm:dev -w server\"",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w shared && npm run build -w client && npm run build -w server",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w client && npm run typecheck -w server",
    "lint": "biome check .",
    "format": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "concurrently": "^8.2.2",
    "typescript": "^5.6.3",
    "vitest": "^3.1.1"
  },
  "engines": {
    "node": ">=20"
  }
}
